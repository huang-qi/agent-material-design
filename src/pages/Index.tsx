import { Navigate } from 'react-router-dom';
import AgentChat from './AgentChat';

// Redirects to the Agent Chat page, which is our main dashboard
const Index = () => {
  return <AgentChat />;
};

export default Index;
